public class Hello3{

    
    // 필드 정의
    int age;
    // int -> 키워드
    // age -> 식별자(변수)


    int myAge = 10;



    // 메서드(함수) 기능수행 역할

    // 두개의 숫자 값을 입력 받아서 높은 값을 구하는 메서드
    // 매서드와 필드의 차이는 괄호가 유,무 차이 있으면 함수
    
    //함수의 속성
        // 매게변수 (필수 X) 
        // 리턴타입 (필수 X)
    
    int maxNumber(int x, int y){ 

        return 0; // -> 함수를 호출 하는 부분

    } // int부터 여기까지 함수의 정의 및 선언된 부분


    // 리턴타입 함수명(매게변수) 블럴{}

    // 입력값 없는 경우
   /*  int maxNumber(){ 

        return 0;
    }

    // 리턴타입없는 경우
     void maxNumber(){ 
        
        int result = maxNumber (10, 20); 

     }  */

}